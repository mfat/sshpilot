"""Example plugins shipped for reference only.

Nothing here is auto-loaded: the loader scans ``builtin/`` and the user
plugin dir, never ``examples/``. To try an example, copy its directory into
the user plugin dir (``$XDG_DATA_HOME/sshpilot/plugins``) and enable it in
Preferences ▸ Plugins.
"""
